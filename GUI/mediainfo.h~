#ifndef C_MEDIAINFO_H_
#define C_MEDIAINFO_H_
 
	#include <stdio.h>
	#include <string.h>
	#include <gst/gst.h>
	#include <gst/pbutils/pbutils.h>


		/* Structure to contain all our information, so we can pass it around */
		typedef struct _CustomData {
			GstDiscoverer *discoverer;
			GMainLoop *loop;
		} CustomData;

	#ifdef __cplusplus
	extern "C" {
	#endif
		void C_Func_MediaInfo (char * filename);
	#ifdef __cplusplus
	}
	#endif
 
#endif /* C_MEDIAINFO_H_ */
